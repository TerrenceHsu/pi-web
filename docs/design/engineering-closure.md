# 本机 Web Agent 工程化收敛

日期：2026-09-07。按用户要求依次实施；不启用公网部署，不自动迁移业务数据或替换运行中服务。

## 顺序和验收

1. 持续运行：Telemetry 周期裁剪保护运行中记录并公开清理错误；产品网关在安装锁内、
   账户运行时启动前对账旧进程崩溃记录；MinerU 心跳/过期探针；
   已回收执行对象释放，维护仅扫描活跃许可/清理债务，授权和发布证据仍持久化。
2. 数据维护：离线一致性备份、校验、恢复到全新目录；Wiki 旧版数据升级须保留原目录，
   明确数据库与文件、凭证及签名密钥边界。先在临时样本演练，不操作业务库。
3. 本地一键门禁：锁文件、静态检查、后端/前端、Evals、Worker 和 Chromium；
   实机 Docker/MinerU 必须显式选择固定镜像及测试目录。CPU/GPU 分档分别记录证据，
   缺少镜像、模型、GPU 或语料时不得标记通过，也不能用旧镜像健康状态代替新版验收。
4. 账号与交付：改密要求当前密码，原子更新并撤销旧登录 Session；前端可操作，
   不提供未认证重置入口。同步状态文档、记录实际门禁并整理提交。

## 进度

- 第一项：113 项相关回归通过；主项目/Worker 静态检查通过。
- 第二项：离线备份恢复、显式 Wiki 兼容迁移/原件重建已实现；50 passed / 1 skipped。
  原件重建只保留旧历史归档，不宣称原 ID/页面/审批无损迁移。操作见数据维护指南。
- 第三项：一键门禁/CI 已实现；Docker 25 项底层 + 12 项 Web 实机通过；
  MinerU 三档解析/销毁与运行中取消/重启恢复通过，GPU 合成表格/OCR 正确，但 CPU 表格质量不通过。
  `runtime_ready=false`，代表性语料验收仍待完成；一键 all 的 15 项检查/恢复全部通过。
- 第四项：改密 API/UI、状态文档完成；交付补充维护/Auth 25 项通过，Chromium 27 项通过。
  代码与报告整理本地提交，不推送、不更换业务配置。
- 已有 Workspace Bash 阶段 5 改动保留，纳入同一本地交付；完整结果见工程化验收报告。
